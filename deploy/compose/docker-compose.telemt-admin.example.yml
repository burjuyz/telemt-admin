# Пример: только telemt-admin. Процесс telemt должен быть доступен по сети
# (например отдельный compose-стек или хост) — укажите telemt_api.base_url в конфиге.
# Если нужен только control API path, держите allow_file_fallback = false и не монтируйте
# telemt.toml внутрь контейнера. Для legacy write-path потребуется отдельный RW mount.
#
# Запуск:
#   cp .env.example .env
#   docker compose up -d
# Рядом с этим файлом ожидаются:
#   - .env
#   - config/telemt-admin.toml
#   - data/

services:
  telemt-admin:
    image: ghcr.io/fgbm/telemt-admin:${TELEMT_ADMIN_VERSION:-latest}
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config/telemt-admin.toml:/etc/telemt-admin/telemt-admin.toml:ro
      - ./data:/var/lib/telemt-admin
    environment:
      TELEMT_ADMIN__RUNTIME__MODE: external
      TELEMT_ADMIN__RUNTIME__LABEL: docker
